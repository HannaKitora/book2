 <% if user_signed_in? %>
  <div>
    <h1>User info</h1>

    <%= image_tag user.get_profile_image(150,150) %>
      <p>name<%= user.name %></p>  
      <p>introduction<%= user.text %></p>
  </div>
  
  <div>
  <h1>New book</h1>
    <%= form_with model: @book do |f| %>
      <h4>Title</h4>
      <%= f.text_field :title %>
      <h4>Opinion</h4>
      <%= f.text_area :body %>
      <%= f.submit 'Create Book' %>
    <% end %>
  </div>
<% end %>


<h1>Books</h1>

